#include<bits/stdc++.h>
using namespace std;

int main(){
    int price[] = { 10, 4, 5, 90, 120, 80 };
    int n = sizeof(price) / sizeof(price[0]);
    int S[n];
    // vector<int> arr = calculateSpan(price, n);
    
    cout << S[0];
    return 0;
}